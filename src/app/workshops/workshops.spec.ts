import { WorkshopsPage } from './workshops';
describe('WorkshopsPage', () => {
  it('should render the Workshops title', () => {
    const page = new WorkshopsPage();
    expect(page).toBeTruthy();
  });
});
