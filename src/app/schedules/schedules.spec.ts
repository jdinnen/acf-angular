import { SchedulesPage } from './schedules';
describe('SchedulesPage', () => {
  it('should render the Schedules title', () => {
    const page = new SchedulesPage();
    expect(page).toBeTruthy();
  });
});
