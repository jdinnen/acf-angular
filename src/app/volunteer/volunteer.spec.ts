import { VolunteerPage } from './volunteer';
describe('VolunteerPage', () => {
  it('should render the Volunteer title', () => {
    const page = new VolunteerPage();
    expect(page).toBeTruthy();
  });
});
